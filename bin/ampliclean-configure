#!/usr/bin/env node
"use strict";
const program = require('commander');
const configurationManager = require('../lib/configuration-manager'); 

program
    .description('configures the cli')
    .parse(process.argv)

configurationManager.configure(); 