#!/usr/bin/env node
"use strict";
const program = require('commander');
const handler = require('../lib/handler-cloudformation'); 

program
    .description('edit configuration settings of the aws account or the project')
    .parse(process.argv)

handler.run(true); 
