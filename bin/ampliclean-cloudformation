#!/usr/bin/env node
"use strict";
const program = require('commander');
const handler = require('../lib/handler-cloudformation'); 

program
    .description('removes all cloudformation stacks')
    .parse(process.argv)

handler.run(true); 
