#!/usr/bin/env node
"use strict";
const program = require('commander');
const handler = require('../lib/handler-pinpoint'); 

program
    .description('removes all pinpoint projects')
    .parse(process.argv)

handler.run(true); 
